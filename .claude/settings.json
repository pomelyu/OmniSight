{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // \"\"' | { read -r f; case \"$f\" in *.py) python -m isort \"$f\" ;; esac; } 2>/dev/null || true",
            "statusMessage": "Running isort..."
          }
        ]
      }
    ]
  }
}
